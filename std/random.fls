// Fls Standard Random Library

// Returns a random floating-point number between 0.0 (inclusive) and 1.0 (exclusive).
export fun randFloat() {
    return random(); // Native function
}

// Returns a random integer between min (inclusive) and max (inclusive).
export fun randInt(min, max) {
    return randomInt(min, max); // Native function
}
